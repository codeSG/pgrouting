#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-dummyDijkstra
            )],
        'documentation' => [qw(
            doc-dummyDijkstra
            )]
    },

);

1;
