#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-codesgDijkstra
            )],
        'documentation' => [qw(
            doc-codesgDijkstra
            )]
    },

);

1;
